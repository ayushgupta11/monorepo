{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "lerna run clean",
    "bootstrap": "lerna bootstrap",
    "build:shared": "lerna run build --scope=@monorepo/shared"
  },
  "devDependencies": {
    "lerna": "^3.19.0"
  }
}